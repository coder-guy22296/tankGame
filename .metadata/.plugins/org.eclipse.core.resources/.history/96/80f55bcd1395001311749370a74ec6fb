package tank;

import javax.swing.*;
import java.awt.*;
import java.util.*;

public class Board extends JPanel {

	Random rng = new Random();
	Rectangle[] rects = new Rectangle[20];
	Tank[] tanks = new Tank[2];
	
	Board() {
		for(int i = 0; i < 20; i++) {
			rects[i] = new Rectangle((rng.nextInt() % 300) + 50, (rng.nextInt() % 300) + 50, (rng.nextInt() % 50) + 50, (rng.nextInt() % 50) + 50);
		}
	}
	
	public void paint(Graphics g) {
		Graphics2D g2d = (Graphics2D) g;
		
	}
	
}
